<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions (normal — auto-granted) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Camera permission (dangerous — requires runtime request) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wake lock for keeping MQTT connection alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_temi_agent"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_temi_agent"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- Temi SDK metadata -->
        <meta-data
            android:name="com.robotemi.sdk.metadata.SKILL"
            android:value="@string/app_name" />
        <meta-data
            android:name="com.robotemi.sdk.metadata.KIOSK"
            android:value="TRUE" />
        <meta-data
            android:name="com.robotemi.sdk.metadata.UI_MODE"
            android:value="1" />
        <meta-data
            android:name="com.robotemi.sdk.metadata.OVERRIDE_NLU"
            android:value="TRUE" />
        <meta-data
            android:name="com.robotemi.sdk.metadata.OVERRIDE_CONVERSATION_LAYER"
            android:value="TRUE" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".mqtt.MqttLifecycleService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false" />

    </application>
</manifest>
